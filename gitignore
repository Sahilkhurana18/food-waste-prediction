# Dependencies
node_modules/
__pycache__/
*.pyc

# Environment files — never commit real secrets
.env
.env.local
*/.env
*/.env.local

# Build output
dist/
build/
*.egg-info/

# ML service artifacts (regenerated by the Dockerfile at build time —
# no need to version-control them, and food_demand.csv can be regenerated)
ml-service/data/food_demand.csv
ml-service/models/*.joblib
ml-service/models/*.json

# OS/editor cruft
.DS_Store
.vscode/
*.log

# Docker
*.pid
