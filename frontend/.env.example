# Base URL of the backend API. Defaults to http://localhost:4000 if unset,
# which is correct when running the whole stack via the root docker-compose
# on the same machine (this runs in the browser, not in the container, so
# it reaches the backend's host-mapped port directly).
VITE_API_BASE_URL=http://localhost:4000
